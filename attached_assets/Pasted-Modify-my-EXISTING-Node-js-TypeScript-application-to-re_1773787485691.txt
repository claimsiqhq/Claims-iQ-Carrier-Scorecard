Modify my EXISTING Node.js + TypeScript application to replace demo logic with a real OpenAI-powered audit engine using a STRICT JSON contract and validation.

CRITICAL RULES:
- DO NOT rebuild the app
- DO NOT refactor unrelated code
- DO NOT change routing, UI structure, or file ingestion
- ONLY replace demo/mock audit logic
- ENSURE the AI output NEVER breaks the UI or email

---

GOAL:

Take the final report text input and:

1. Send it to OpenAI
2. Use a strict audit prompt
3. Parse the JSON response
4. Validate the structure
5. Fallback safely if invalid
6. Return structured audit data to the existing UI and email system

---

==================================================
STEP 1 — INSTALL + INIT OPENAI
==================================================

Install:
npm install openai

Initialize:

import OpenAI from "openai";

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});

---

==================================================
STEP 2 — CREATE JSON CONTRACT (TYPES)
==================================================

Create file:
/types/audit.ts

Add:

export interface AuditResult {
  overall_score: number;
  technical_score: number;
  presentation_score: number;

  section_scores: {
    coverage_clarity: number;
    scope_completeness: number;
    estimate_accuracy: number;
    documentation_support: number;
    financial_accuracy: number;
    carrier_risk: number;

    file_stack_order: number;
    payment_match: number;
    estimate_operational_order: number;
    photo_organization: number;
    da_report_quality: number;
    fa_report_quality: number;
    policy_provisions: number;
  };

  risk_level: "LOW" | "MEDIUM" | "HIGH";

  approval_status:
    | "APPROVE"
    | "APPROVE WITH MINOR CHANGES"
    | "REQUIRES REVIEW"
    | "REJECT";

  critical_failures: string[];
  key_defects: string[];
  presentation_issues: string[];
  carrier_questions: string[];

  deferred_items: any[];
  invoice_adjustments: any[];
  scope_deviations: any[];
  unknowns: string[];

  executive_summary: string;
}

---

==================================================
STEP 3 — CREATE VALIDATION FUNCTION
==================================================

Create file:
/services/validateAudit.ts

Add:

export function validateAuditResult(data: any): boolean {
  if (!data) return false;

  const required = [
    "overall_score",
    "technical_score",
    "presentation_score",
    "section_scores",
    "risk_level",
    "approval_status",
    "executive_summary"
  ];

  for (const field of required) {
    if (!(field in data)) return false;
  }

  if (typeof data.overall_score !== "number") return false;
  if (typeof data.technical_score !== "number") return false;
  if (typeof data.presentation_score !== "number") return false;

  return true;
}

---

==================================================
STEP 4 — CREATE FALLBACK RESULT
==================================================

Create function:

function getFallbackAudit() {
  return {
    overall_score: 0,
    technical_score: 0,
    presentation_score: 0,
    section_scores: {
      coverage_clarity: 0,
      scope_completeness: 0,
      estimate_accuracy: 0,
      documentation_support: 0,
      financial_accuracy: 0,
      carrier_risk: 0,
      file_stack_order: 0,
      payment_match: 0,
      estimate_operational_order: 0,
      photo_organization: 0,
      da_report_quality: 0,
      fa_report_quality: 0,
      policy_provisions: 0
    },
    risk_level: "HIGH",
    approval_status: "REQUIRES REVIEW",
    critical_failures: ["Audit processing failed"],
    key_defects: [],
    presentation_issues: [],
    carrier_questions: [],
    deferred_items: [],
    invoice_adjustments: [],
    scope_deviations: [],
    unknowns: [],
    executive_summary: "Audit could not be completed due to a processing error."
  };
}

---

==================================================
STEP 5 — CREATE PROMPT CONSTANTS
==================================================

Create file:
/services/prompts.ts

Add:

export const SYSTEM_PROMPT = `
You are a senior insurance carrier audit reviewer.

You must return STRICT JSON ONLY.

Do not include explanations or markdown.

If something is missing or unclear, treat it as a defect.
`;

export const USER_PROMPT_TEMPLATE = `
Evaluate this finalized claim report.

Return ONLY valid JSON.

Score using:

TECHNICAL (80):
- Coverage
- Scope
- Estimate
- Documentation
- Financial
- Risk

PRESENTATION (20):
- File stack order
- Payment match
- Estimate order
- Photo organization
- DA report quality
- FA report quality
- Policy provisions

Return JSON with ALL required fields.

REPORT:
{{REPORT}}
`;

---

==================================================
STEP 6 — CREATE AUDIT FUNCTION
==================================================

Create file:
/services/audit.ts

Add:

import { SYSTEM_PROMPT, USER_PROMPT_TEMPLATE } from "./prompts";
import { validateAuditResult } from "./validateAudit";

export async function runFinalAudit(reportText: string) {
  const prompt = USER_PROMPT_TEMPLATE.replace("{{REPORT}}", reportText);

  const response = await openai.chat.completions.create({
    model: "gpt-5",
    temperature: 0,
    messages: [
      { role: "system", content: SYSTEM_PROMPT },
      { role: "user", content: prompt }
    ]
  });

  const content = response.choices[0].message.content;

  let parsed;

  try {
    parsed = JSON.parse(content);
  } catch (e) {
    console.error("Invalid JSON:", content);
    return getFallbackAudit();
  }

  if (!validateAuditResult(parsed)) {
    console.error("Invalid structure:", parsed);
    return getFallbackAudit();
  }

  parsed.overall_score =
    parsed.technical_score + parsed.presentation_score;

  return parsed;
}

---

==================================================
STEP 7 — REPLACE DEMO LOGIC
==================================================

Find existing code:

return demoResult;

Replace with:

const audit = await runFinalAudit(reportText);
return audit;

---

==================================================
STEP 8 — LOGGING
==================================================

Add logs:

console.log("Audit started");
console.log("Audit completed");

---

FINAL RESULT:

- Demo logic removed
- OpenAI integrated
- Strict JSON enforced
- UI + email never break
- System production-safe